# Time-stamp: <2017-06-07 15:52:48 cp983411>

The design matrices are in files session[1-9].csv.

The order of variables is (from column 1 to 7):  wordrate freq  bottomup  leftcorner  mwe rms f0

They were create with the script *create_design_matrices.sh* which extracts the hrf regressors from the N_XXX_reg.csv files and abuts them.

The N_XXX_reg.csv files were generated inside the 'preprocess' subdirectories.

